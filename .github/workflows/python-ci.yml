name: Python CI               # Название workflow
on:                           # Когда запускать:
  push:                       # - при загрузке кода
    branches: [ master ]        # - только для веток master
  pull_request:               # - при создании PR
    branches: [ master ]        # - только в master

jobs:                         # Задачи которые нужно выполнить
  test:                       # Задача "test"
    runs-on: ubuntu-latest    # На какой ОС запускать

    steps:                    # Шаги выполнения
    # Каждый step - это одно действие
    - name: Выкачать код
      uses: actions/checkout@v4

    - name: Установить Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Установить проект
      run: |
        pip install -e .

    - name: Установить зависимости
      run: |
        python -m pip install --upgrade pip
        pip install pytest pylint

    - name: Запустить тесты
      run: |
        pytest tests/ -v

    - name: Проверить код линтером
      run: |
        pylint src/ tests/
